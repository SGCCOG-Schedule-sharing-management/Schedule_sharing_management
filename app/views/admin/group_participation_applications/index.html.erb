<% if flash[:success].present? %>
  <div class="alert alert-success">
    <%= flash[:success] %>
  </div>
<% end %>

<h6>Adminグループ申請一覧</h6>

<% @requests_all.each do |applying| %>

申請日時：<%= applying.created_at.strftime('%Y-%m-%d  %p%I:%M') %></br>
ユーザー：<%= applying.user.nickname %></br>
本番予定日：<%= applying.group.actual_date.present? ? applying.group.actual_date.strftime('%Y-%m-%d') : '未定' %></br>
グループタイトル：<%= applying.title %></br>
コメント：<%= applying.comment %></br>
申請ステータス：<%= applying.application_status_i18n %></br>

<%= link_to '詳細', admin_group_participation_application_path(applying.id), class: 'btn btn-primary' %></br>
-----

<% end %>